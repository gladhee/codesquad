public enum TokenType {

    // Keyword
    CREATE,
    TABLE,
    INSERT,
    INTO,
    VALUES,
    UPDATE,
    SET,
    DELETE,
    FROM,
    WHERE,

    // Symbol
    LEFT_PAREN,
    RIGHT_PAREN,
    COMMA,
    SEMICOLON,
    EQUALS,
    GREATER,
    LESS,
    DOUBLE_QUOTE,

    // literal
    IDENTIFIER,

    // End of Input
    EOF

}
